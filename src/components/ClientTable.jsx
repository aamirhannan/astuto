'use client';
import React from 'react';
import DataTable from './DataTable';

const ClientTable = ({ data, config }) => {
    return <DataTable data={data} config={config} />;
};

export default ClientTable; 